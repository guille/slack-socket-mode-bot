# frozen_string_literal: true

RSpec.describe Slack::Socket::Mode::Bot do
  it "has a version number" do
    expect(Slack::Socket::Mode::Bot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
